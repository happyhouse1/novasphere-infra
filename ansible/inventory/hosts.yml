all:
  children:
    webservers:
      hosts:
        novasphere-web:
          ansible_host: 13.39.105.85
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
  vars:
    app_name: "NovaSphere"
    env: "Production"
    app_version: "1.0.0"
    nginx_root: "/var/www/novasphere"
    web_packages:
      - nginx
      - git
